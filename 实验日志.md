# 实验日志

## 2026-05-24 Decoder Fusion 复测

### 结果

评估标准：test split，IoU = 0.5，遍历 confidence threshold，取全局 micro-F1 最大值对应的 confidence。训练日志和独立评估都按这个标准看。

选中的 UV teacher baseline：

| 权重 | F1 | Confidence | Precision | Recall | mAP50:95 |
|---|---:|---:|---:|---:|---:|
| `output/teacheruv/checkpoint_best_total.pth` | 0.7639 | 0.41 | 0.7991 | 0.7317 | 0.5010 |

选中的 decoder fusion：

| 权重 | F1 | Confidence | Precision | Recall | mAP50:95 |
|---|---:|---:|---:|---:|---:|
| `output/ref_uv_decoder_fusion_2x4090/20260523_152943/checkpoint_best_total.pth` | 0.7648 | 0.42 | 0.8148 | 0.7207 | 0.5207 |

decoder fusion 相比 UV teacher：

| 指标 | 变化 |
|---|---:|
| F1 | +0.0010 |
| Precision | +0.0157 |
| Recall | -0.0110 |
| mAP50:95 | +0.0197 |

选中 decoder fusion 的类别 AP50:95：

| 类别 | AP50:95 |
|---|---:|
| NPML | 0.6825 |
| PML | 0.6674 |
| PM | 0.2121 |

原始复测汇总：

`agent_outputs/retest_decoder_fusion_20260524/comparison.json`

### 相对 UV Baseline 的模块化改进

UV teacher baseline 是纯 UV 输入的 RF-DETR 检测器。decoder fusion 实验保留 UV 检测器作为主语义路径，只把 white reference 信息通过 reference prior 和 decoder 内部融合引入。下面按后续消融实验可以单独开关的模块记录。

| 编号 | 模块 | 当前实现 | 后续消融方式 |
|---:|---|---|---|
| 1 | white edge 结构边缘 | 使用 white 图像灰度 Sobel 边缘，通道名 `white_edge`。提供叶片边界、叶脉和结构定位线索。 | 去掉 `white_edge` 通道，观察结构信息对 F1、recall、定位 mAP 的影响。 |
| 2 | RNFR 残差 | 使用 `log((UV + eps) / (White + eps))`，按叶片区域 median 居中并缩放，通道名 `rnfr_r/rnfr_g/rnfr_b`。提供 UV 相对白光的荧光残差信息。 | 去掉 `rnfr_r/g/b` 三个通道，或只保留 RNFR 做单独对照。 |
| 3 | 蓝色响应增强 | 使用 ExB residual：`2 * B - G - R`，比较 UV 和 white 的蓝色响应差异，通道名 `exb_residual`。 | 去掉 `exb_residual`，验证蓝色增强对 PM/PML 小目标和弱病斑的作用。 |
| 4 | white raw RGB | 直接输入 white 原图 RGB，并乘 leaf confidence，通道名 `white_raw_r/white_raw_g/white_raw_b`。提供白光纹理、叶片结构和背景参考。 | 去掉 raw white RGB，只保留 residual/edge；或反过来只保留 raw white RGB。 |
| 5 | 叶片 mask / leaf confidence | 从 white 图像估计叶片区域，把 RNFR、ExB、edge、raw white 都限制在叶片可信区域内，减少背景和标定板干扰。 | 关闭 leaf confidence 乘法，或替换为全 1 mask，观察背景 shortcut 是否影响结果。 |
| 6 | reference tokenizer | 用轻量卷积 tokenizer 把 8 通道 prior 转成多尺度 reference feature，对齐 RF-DETR feature levels。 | 替换为更浅/更深 tokenizer；或冻结 tokenizer；或用 1x1 投影做最小 tokenizer 对照。 |
| 7 | alignment 对齐模块 | `MisalignmentAwareReferenceAlignment` 用学习到的局部 offset 对齐 reference feature 和 UV feature，缓解 UV/white 小幅错位。 | 去掉 alignment，直接把 tokenizer 输出送入 decoder；或把 offset 固定为 0。 |
| 8 | decoder fusion | 在 decoder layer 内增加 reference deformable cross-attention 分支，而不是在最终 head 前融合。UV 原始 decoder 路径保留。 | 关闭 reference cross-attention，退化为 UV-only；或比较 head fusion / decoder fusion。 |
| 9 | query-level gate + beta | 每个 object query 预测 reference 使用强度，reference residual 由 gate 和 capped beta 控制，初始状态接近 UV-only。 | 去掉 gate、固定 gate=1、固定 beta、调大 beta 初值，观察 reference 是否真正被使用。 |
| 10 | UV teacher preservation | 训练时使用冻结 UV teacher，对 student logits 和 boxes 加 preservation loss，减少 fusion 训练遗忘 UV-only 行为。 | 去掉 teacher loss；或降低 teacher loss 权重；或只保留 cls/box 其中一种 teacher loss。 |

当前 reference prior 通道顺序：

| 通道序号 | 通道名 | 所属模块 |
|---:|---|---|
| 1 | `rnfr_r` | RNFR 残差 |
| 2 | `rnfr_g` | RNFR 残差 |
| 3 | `rnfr_b` | RNFR 残差 |
| 4 | `exb_residual` | 蓝色响应增强 |
| 5 | `white_edge` | white edge 结构边缘 |
| 6 | `white_raw_r` | white raw RGB |
| 7 | `white_raw_g` | white raw RGB |
| 8 | `white_raw_b` | white raw RGB |

当前理解：

按 max-F1 标准，选中的 decoder fusion checkpoint 略高于 UV teacher，但优势很小。提升主要来自 precision 增加，recall 有下降。mAP50:95 提升更明显，说明定位或排序质量相比最终 F1 工作点改善更清楚。

## 2026-05-24 DINOv2 PCA 与高范数背景依赖检查

### 目的

担心 RF-DETR 的 DINOv2 backbone 可能像一些 ViT/自监督模型一样，过度利用背景区域而不是叶片/病斑本身。先用白光图像做 encoder patch 特征 PCA 和 feature norm 诊断，观察是否存在明显的背景 shortcut 或高范数背景伪影。

### 数据和权重

- 图像：`data/val/white` 前 12 张白光验证图。
- 标注：`data/val/uv/_annotations.coco.json`，通过文件名中的 `white` / `uv` token 匹配。
- 权重：`output/teacheruv/checkpoint_best_total.pth`。
- 模型：`RFDETRSmall`，只加载 checkpoint 中的 `backbone.0.encoder` 权重做 backbone 特征可视化。

### 输出文件

- PCA 可视化：`agent_outputs/dinov2_pca_visualization/results_teacheruv_white`
- 无框 PCA 可视化：`agent_outputs/dinov2_pca_visualization/results_teacheruv_white_nobox`
- 高范数分析：`agent_outputs/dinov2_pca_visualization/norm_teacheruv_white`
- PCA 脚本：`agent_outputs/dinov2_pca_visualization/visualize_dinov2_pca.py`
- feature norm 脚本：`agent_outputs/dinov2_pca_visualization/analyze_feature_norms.py`

### 高范数统计结论

基于保守粗分区域：明显暗区域记为 background，明显绿色/高饱和区域记为 leaf，不确定区域不参与 leaf/background 判断。

| 指标 | 平均值 | 观察 |
|---|---:|---|
| background patch fraction | 0.2520 | 背景约占 25.2% patch |
| top 5% norm 中 background fraction | 0.0817 | 高范数 patch 中背景只占约 8.2% |
| background enrichment | 0.3531 | 背景没有高范数富集 |
| leaf patch fraction | 0.6393 | 叶片约占 63.9% patch |
| top 5% norm 中 leaf fraction | 0.8072 | 高范数 patch 更偏向叶片 |
| leaf enrichment | 1.2944 | 叶片区域有一定高范数富集 |

逐图检查：

- `background enrichment > 1`：0 / 12。
- `leaf enrichment > 1`：11 / 12。

因此当前 12 张白光图里没有看到明显“高范数集中在背景”的证据。高范数更常落在叶片、叶脉、叶片边缘和强纹理区域。

### PCA 观察结论

PCA 图显示 DINOv2 encoder 能把暗背景编码成比较稳定的一类 patch，说明背景确实在 backbone 特征空间中可分，存在被模型利用为 shortcut 的潜在风险。但这只是“背景可分”，还不能证明最终检测判断依赖背景。

同时，PCA 和 norm panel 中能看到少量非叶片亮结构会产生较强响应，例如塑料网、白色柱子、强反光区域。这类更像高亮/高对比背景物体造成的局部伪影，而不是暗背景整体支配。

### 当前判断

当前证据支持：

1. 没有明显的高范数背景伪影。
2. backbone 对叶片/叶脉/强纹理区域响应更强。
3. 背景区域在 PCA 特征空间中可分，存在背景 shortcut 风险，但还不能直接判定模型依靠背景判断。
4. 更需要警惕的是白色柱子、塑料网、反光等高对比非叶片结构，而不是单纯暗背景。

### 后续建议

下一步不要只看 PCA，应做遮挡/背景抑制消融：

| 实验 | 输入处理 | 目的 |
|---|---|---|
| 原图验证 | 不处理背景 | 作为 baseline |
| 背景抑制验证 | 只抑制高置信背景，不硬抠叶片 | 看减少背景是否提升或降低 mAP/F1 |
| 只保留背景验证 | 遮住叶片/目标，只留背景 | 如果仍有大量预测，说明背景 shortcut 明显 |
| 只保留叶片验证 | 背景置灰/模糊，叶片区域保留 | 看模型是否主要依赖叶片结构 |

正式比较时，训练、验证、最终推理的数据处理流程应该一致；“训练原图、验证时才扣背景”只能作为诊断，不适合作为最终模型效果结论。

## 2026-05-24 白光叶片 mask 纯色背景 UV 单模态训练

### 目的

验证“模型是否过度利用背景 shortcut”的更直接方案：先用白光图像估计叶片区域，把背景替换成纯黑，再把同一个 mask 同步应用到配对的 UV 图像。训练、验证、测试都使用同一种预处理流程，避免出现“训练原图、验证抠背景”导致的数据分布不一致。

### 数据预处理

- 源数据：`data`
- 输出数据：`agent_outputs/leaf_solid_background_experiment/datasets/uv_leaf_solid_black_jpg`
- 生成脚本：`agent_outputs/leaf_solid_background_experiment/make_leaf_solid_dataset.py`
- mask 来源：白光图像。
- mask 模式：`green-leaf`，比原先 reference prior 里的宽松 leaf confidence 更严格，尽量只保留绿色/高饱和连通叶片区域。
- 背景颜色：`0,0,0` 纯黑。
- 文件格式：JPG。
- RF-DETR 训练读取的标注 `file_name` 指向处理后的 `uv/*.jpg`；处理后的 `white/*.jpg` 同时保留，便于检查和后续 paired/ref-UV 实验。

数据规模：

| split | 图像数 | mask fraction 均值 | mask fraction 范围 |
|---|---:|---:|---:|
| train | 791 | 0.7746 | 0.2487 - 0.9649 |
| valid | 54 | 0.7601 | 0.2617 - 0.9239 |
| test | 54 | 0.7979 | 0.5964 - 0.9552 |

预览图保存在：`agent_outputs/leaf_solid_background_experiment/datasets/uv_leaf_solid_black_jpg/_previews`。

### 训练设置

使用 `projects/ref_uv_detr/train_uv_teacher.py` 的 UV teacher 默认参数，保持和最近 teacher UV 训练一致：

- RF-DETR small
- resolution 560
- epochs 180
- batch size 10 / GPU
- grad accumulation 2
- 2 张 RTX 4090，DDP
- lr 3.5e-4
- lr_encoder 1.75e-4
- cosine scheduler，warmup 10 epoch，lr_min_factor 0.35
- early stopping patience 90
- multi-scale 开启

初始化方式符合本次设想：`backbone_only_rfdetr=True`，从官方 `rf-detr-small.pth` 只加载 `backbone.0.encoder` 权重。日志确认加载 223 个 encoder key，projector / decoder / query / head 等检测相关模块保持随机初始化。

训练输出：

- 输出目录：`output/uv_leaf_solid_black_teacher_2x4090/20260524_081653`
- 训练日志：`agent_outputs/leaf_solid_background_experiment/train_uv_leaf_solid_20260524_081653.log`
- 后台主进程 PID：`4166279`

### 当前状态

训练已经成功启动并进入正常 DDP 训练。启动日志显示 train/valid COCO 数据集均可正常构建，`metrics.csv` 和 `last.ckpt` 已写出。

截至 epoch 1，检测头仍处在随机初始化后的早期训练阶段，mAP/F1 接近 0 属于预期现象，不能据此判断最终效果。需要等至少若干十个 epoch 或 best checkpoint 出现后，再和 `output/teacheruv/checkpoint_best_total.pth` 以及 ref-UV fusion 实验做同口径比较。

### 当前判断

这个实验比单纯 PCA 更适合判断背景依赖：如果纯色背景 UV teacher 最终效果接近或高于原始 UV teacher，说明背景信息不是主要必要条件，甚至背景可能有干扰；如果效果明显下降，则需要进一步区分是叶片 mask 误删有效信息，还是模型确实利用了背景/环境上下文。当前 mask 不是人工精标，少量白色柱子、塑料网或强反光结构仍可能被保留或误处理，因此最终结论需要结合预览图和错误案例分析。
